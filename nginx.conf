# nginx.conf
server {
    listen 80;
    
    # 所有的请求都指向 /usr/share/nginx/html 目录
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # 这一行至关重要！解决 React 刷新页面报 404 的问题
        try_files $uri $uri/ /index.html;
    }
}