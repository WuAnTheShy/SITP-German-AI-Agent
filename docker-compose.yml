version: '3.8'

services:
  backend:
    build: ./backend           # 🔴 修改点：指向 backend 目录
    container_name: german-backend
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    restart: always

  frontend:
    build: ./frontend          # 🔴 修改点：指向 frontend 目录
    container_name: german-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always